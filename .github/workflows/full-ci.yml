name: Full CI Check

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [ main, master, develop ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  game-logic-tests:
    name: Game Logic Tests
    needs: unit-tests
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        pip install pytest
        
    - name: Run critical game logic tests
      run: |
        echo "=== 运行关键游戏逻辑测试 ==="
        
        # 测试交易系统
        python -m pytest test_forest.py::TestForestGame::test_trade_success -v
        python -m pytest test_forest.py::TestForestGame::test_trade_fail_exceeds_limit -v
        
        # 测试捕食系统
        python -m pytest test_forest.py::TestForestGame::test_hunt_success -v
        python -m pytest test_forest.py::TestForestGame::test_hunt_fail_reverse -v
        
        # 测试规则系统
        python -m pytest test_forest.py::TestForestGame::test_check_restraint_12_players -v
        python -m pytest test_rules.py::TestRestraintRules::test_rank_restraint_chain -v
        
        # 测试具体案例：红桃K捕食黑桃Q
        echo "=== 测试具体案例：红桃K捕食黑桃Q ==="
        python << 'END'
	import sys
	sys.path.append('.')
	try:
	    from forest import Game, Player, CardRank, CardSuit
	except ImportError:
	    import importlib.util
	    spec = importlib.util.spec_from_file_location('forest', 'forest.py')
	    forest = importlib.util.module_from_spec(spec)
	    spec.loader.exec_module(forest)
	    Game = forest.Game
	    Player = forest.Player
	    CardRank = forest.CardRank
	    CardSuit = forest.CardSuit
	
	# 测试红桃K捕食黑桃Q
	game = Game()
	game.player_count = 12
	
	player1 = Player(1)  # 红桃K
	player2 = Player(2)  # 黑桃Q
	
	player1.rank = CardRank.K
	player1.suit = CardSuit.HEART
	player2.rank = CardRank.Q
	player2.suit = CardSuit.SPADE
	
	result = game._check_restraint(player1, player2)
	print(f'克制关系结果: {result}')
	print(f'期望: 1 (红桃K应该克制黑桃Q, 因为K>Q)')
	assert result == 1, f'测试失败: 红桃K应该克制黑桃Q，但得到{result}'
	print('✅ 测试通过: 红桃K克制黑桃Q')
        END
        
  integration-tests:
    name: Integration Tests
    needs: game-logic-tests
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Run full game simulation
      run: |
        echo "=== 运行完整游戏模拟 ==="
        python -c "
import sys
sys.path.append('.')

# 导入游戏模块
try:
    from forest import Game
except ImportError:
    import importlib.util
    spec = importlib.util.spec_from_file_location('forest', 'forest.py')
    forest = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(forest)
    Game = forest.Game

# 创建游戏实例
game = Game()
game.player_count = 8
game.players = [Game.Player(i+1) for i in range(8)]
game._assign_identities()

print('游戏初始化完成')
print(f'玩家数量: {len(game.players)}')
print(f'Joker数量: {sum(1 for p in game.players if p.rank == game.CardRank.JOKER)}')

# 验证所有玩家都有身份
for player in game.players:
    assert player.rank is not None, f'玩家{player.no}没有点数身份'
    assert player.suit is not None, f'玩家{player.no}没有花色身份'
    print(f'玩家{player.no}: {player.suit.value if player.suit.value != \"Joker\" else \"\"}{player.rank.value if player.rank.value != \"Joker\" else \"Joker\"}')

print('✅ 集成测试通过')
        "
        
    - name: Verify export functionality
      run: |
        echo "=== 测试导出功能 ==="
        python -c "
import sys
sys.path.append('.')
import os
import tempfile

try:
    from forest import Game
except ImportError:
    import importlib.util
    spec = importlib.util.spec_from_file_location('forest', 'forest.py')
    forest = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(forest)
    Game = forest.Game

# 创建临时目录测试导出
with tempfile.TemporaryDirectory() as tmpdir:
    original_cwd = os.getcwd()
    os.chdir(tmpdir)
    
    try:
        game = Game()
        game.player_count = 6
        game.players = [Game.Player(i+1) for i in range(6)]
        game._assign_identities()
        
        # 添加一些操作记录
        game.records.append('测试记录1')
        game.records.append('测试记录2')
        
        # 测试导出
        game.export_data()
        
        # 检查文件是否创建
        import glob
        txt_files = glob.glob('*.txt')
        assert len(txt_files) > 0, '导出文件未创建'
        print(f'找到导出文件: {txt_files[0]}')
        
        # 检查文件内容
        with open(txt_files[0], 'r', encoding='utf-8') as f:
            content = f.read()
            assert '游戏数据导出' in content
            assert '测试记录1' in content
            print('✅ 导出功能测试通过')
            
    finally:
        os.chdir(original_cwd)
        "
        
  final-validation:
    name: Final Validation
    needs: [game-logic-tests, integration-tests]
    runs-on: ubuntu-latest
    
    steps:
    - name: Check all jobs passed
      run: |
        echo "🎉 所有检查通过!"
        echo "游戏逻辑: ✅"
        echo "集成测试: ✅"
        echo ""
        echo "可以安全地合并代码"
